html
	head
		block title
		meta(name="viewport" content="width=device-width, initial-scale=1.0")
		link(rel="icon" href="images/icons/favicon.png")
		link(rel="stylesheet" href="https://necolas.github.io/normalize.css/7.0.0/normalize.css")
		link(rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous")
		link(rel="stylesheet" type="text/css" href="css/styles.css")
		link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:700")
		link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto+Condensed")
	
		block styles
	body
		nav.navbar.navbar-inverse
			.container-fluid
				.navbar-header
					button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1', aria-expanded='false')
						span.sr-only Toggle navigation
						span.icon-bar
						span.icon-bar
						span.icon-bar
					a.navbar-brand(href='/') GeMN
					img(class='mini-ancora' src='/images/ancora_logo.png')
				#bs-example-navbar-collapse-1.collapse.navbar-collapse
					ul.nav.navbar-nav.navbar-right.menua
						if (cookie.perfil == 'cliente')			
							li(class=cliente)
								a(href='/cliente')
									| Cliente
									span.sr-only (current)
							li(class=logout)
								a(href='/logout')
									| Logout
									span.sr-only (logout)
						else if (cookie.perfil == 'usuario')
							li(class=usuario)
								a(href='/usuario')
									| Usuário
									span.sr-only (current)
							li(class=logout)
								a(href='/logout')
									| Logout
									span.sr-only (logout)
						else if (cookie.perfil == 'empresa')
							li(class=empresa)
								a(href='/empresa')
									| Empresa
									span.sr-only (current)
							li(class=logout)
								a(href='/logout')
									| Logout
									span.sr-only (logout)
						else
							li(class=login)
								a(href='/')
									| Login
									span.sr-only (current)	
							li(class=cadastro)
								a(href='/cadastro')
									| Cadastro
									span.sr-only (current)	

		block content

		block scripts
			script(type="text/javascript" src="https://code.jquery.com/jquery-3.1.1.js")
			script(src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous")
			script(type="text/javascript" src="js/app.js")
